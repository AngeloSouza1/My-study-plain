# frozen_string_literal: true

RSpec.describe Minimize::Cash::Flow do
  it "has a version number" do
    expect(Minimize::Cash::Flow::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
